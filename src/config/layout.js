/**
 * Shared layout constants.
 * TAB_BAR_HEIGHT: approximate height of the floating tab bar.
 * Add this as paddingBottom to any scrollable screen that sits behind the tab bar.
 */
export const TAB_BAR_HEIGHT = 80;
